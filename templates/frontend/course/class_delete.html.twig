{% extends '@Forumify/frontend/base.html.twig' %}
{% block body %}
    {% include '@Forumify/admin/components/confirm.html.twig' with {
        warningText: 'perscom.course.class.delete'|trans({ class: class.title }),
        confirmPath: path('perscom_course_class_delete', { id: class.id, confirmed: 1 }),
        cancelPath: path('perscom_course_class_view', { id: class.id })
    } only %}
{% endblock %}
