{% extends '@Forumify/frontend/base.html.twig' %}
{% block body %}
    {% include '@Forumify/admin/components/confirm.html.twig' with {
        warningText: 'perscom.aar.delete'|trans({ unit: aar.unit.name, mission: aar.mission.title }),
        confirmPath: path('perscom_aar_delete', { id: aar.id, confirmed: 1 }),
        cancelPath: path('perscom_aar_view', { id: aar.id })
    } only %}
{% endblock %}
