{% extends '@Forumify/frontend/base.html.twig' %}
{% block body %}
    {% include '@Forumify/admin/components/confirm.html.twig' with {
        warningText: 'perscom.mission.delete'|trans({ mission: mission.title }),
        confirmPath: path('perscom_missions_delete', { id: mission.id, confirmed: 1 }),
        cancelPath: path('perscom_missions_view', { id: mission.id })
    } only %}
{% endblock %}
